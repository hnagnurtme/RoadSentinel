services:
  backend:
    container_name: roadsentinel-backend
    build:
      context: .
      dockerfile: Dockerfile
    image: ${BACKEND_IMAGE:-roadsentinel-backend:local}
    ports:
      - "8000:8000"
    environment:
      APP_ENV: development
      APP_HOST: 0.0.0.0
      APP_PORT: 8000
      DATABASE_URL: sqlite:////app/data/app.db
      SQL_ECHO: "false"
    volumes:
      - backend_data:/app/data
    restart: unless-stopped

volumes:
  backend_data:
